apiVersion: skaffold/v1beta12
kind: Config
build:
  artifacts:
  - image: kanatakita/tabemap-api
    context: .
    docker:
      dockerfile: ./kubernetes/container/Dockerfile
  tagPolicy:
    gitCommit:
      variant: Tags
    #envTemplate:
    #  template: latest
  local:
    push: true
    useBuildkit: true
deploy:
  kubectl:
    manifests:
      - ./kubernetes/manifest/manifest.yaml
